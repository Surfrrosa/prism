{
  "name": "prism",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "build:dev": "node build.mjs --dev",
    "data": "node scripts/build-sources.mjs",
    "icons": "node scripts/generate-icons.mjs",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/chrome": "0.1.40",
    "esbuild": "0.28.0",
    "sharp": "0.34.5",
    "typescript": "6.0.2"
  },
  "dependencies": {
    "idb": "8.0.3"
  }
}
